| inputaoc 2025-03.txt
| eval
    i = mvreverse(mvrange(0, 12)),
    bank = _raw,
    jolts = ""
| foreach mode=multivalue i [
    | eval
        x = substr(bank, 0, len(bank) - '<<ITEM>>'),
        x = max(split(x, "")),
        bank = replace(bank, printf("^\d*?%s", x), ""),
        jolts = jolts.x
]
| stats sum(jolts)
